"use client";

import React from "react";

interface TokenDistributionListProps {
  data: {
    tokenId: number;
    tokenName: string;
    amountOwned: number;
    marketValue: number;
  }[];
}

const TokensDistributionList = (props: TokenDistributionListProps) => {
  return <></>;
};

export default TokensDistributionList;
